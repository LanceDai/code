#include <bits/stdc++.h>

using namespace std;

int main() {
#ifndef ONLINE_JUDGE
    freopen("data.in", "r", stdin);
    freopen("data.ou", "w", stdout);
#endif
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

}